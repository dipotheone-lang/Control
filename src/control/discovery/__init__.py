"""Phase 0 — discovery & baseline (charter §6).

RUN_MODE=DISCOVERY. Sends nothing. Read-only on source data; writes only
to CONTROL_ROOT/discovery/. Every limitation is recorded, never papered
over: an unreadable archive is a listed fact, not a silent skip (§1.1).
"""
